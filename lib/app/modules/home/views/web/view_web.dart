import 'package:flutter/material.dart';

class ViewWeb extends StatelessWidget {
  const ViewWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: SizedBox(
        height: 50,
        width: 50,
        child: Text("Tampilan Web")),
    );
  }
}
